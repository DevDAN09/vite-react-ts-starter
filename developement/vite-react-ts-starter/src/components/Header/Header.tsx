import {
    HeaderBlock,
    HeaderContent
} from "./Header.style"

const Header: React.FC = () => {
    return (
        <HeaderBlock>
            <HeaderContent>
                <p>logo</p>
            </HeaderContent>
        </HeaderBlock>
    )
}

export default Header;